#pragma config(Hubs,  S1, HTMotor,  HTMotor,  HTMotor,  none)
#pragma config(Sensor, S1,     ,               sensorI2CMuxController)
#pragma config(Sensor, S2,     irSeeker,       sensorI2CCustom)
#pragma config(Motor,  mtr_S1_C1_1,     left,          tmotorTetrix, openLoop, encoder)
#pragma config(Motor,  mtr_S1_C1_2,     right,         tmotorTetrix, openLoop, encoder)
#pragma config(Motor,  mtr_S1_C2_1,     liftLeft,      tmotorTetrix, openLoop, encoder)
#pragma config(Motor,  mtr_S1_C2_2,     liftRight,     tmotorTetrix, openLoop, encoder)
#pragma config(Motor,  mtr_S1_C3_1,     scoop,         tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_2,     picollo,       tmotorTetrix, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#include "library.c"
#include "C:\Program Files (x86)\Robomatter Inc\ROBOTC Development Environment 4.X\Sample Programs\NXT\3rdPartyDriverLibrary\include\hitechnic-irseeker-v2.h"

// Global variables
int ir[6];

// Check whether we arrived at the IR beacon
bool hasArrived(void)
{
	const int arrivedThreshold = 105; // The value the IR beacon gets when we arrive
	for (int i = 1; i <= 5; i++)
	{
		if (ir[i] > arrivedThreshold) return true;
	}
	return false;
}

void liftHeight(int height, int speed = liftAutoSpeed)
{
	nMotorEncoder[liftLeft] = 0;
	nMotorEncoder[liftRight] = 0;
	while (abs(encodersToTurns(nMotorEncoder[liftLeft]) * spoolCircumference) < height)
	{
		int goingDown = height > 0 ? 1 : -1;
		setLift(speed * goingDown);
	}
	setLift(0);
}

task main()
{

	// Go somewhat slowly so we can pick up IR
	const int maxSpeed = 60;

	bool wentForwardAlready = false;

	int lastLeft = 1;

	motor[left] = maxSpeed;
	motor[right] = maxSpeed;
	wait1Msec(1500);
	motor[left] = 0;
	motor[right] = 0;

	while(true)
	{

		if (!hasArrived())
		{

			HTIRS2readAllACStrength(irSeeker, ir[1], ir[2], ir[3], ir[4], ir[5]);

			int direction = HTIRS2readACDir(irSeeker);

			// These variables that are used only once don't exist, because RobotC can't hang
			if (direction == 0)
			{
				if (!wentForwardAlready)
				{
					motor[left] = maxSpeed;
					motor[right] = maxSpeed;
					wait1Msec(1000);
					wentForwardAlready = true;
				}
				else
				{
					motor[left] = lastLeft * maxSpeed;
					motor[right] = -1 * lastLeft * maxSpeed;
				}
			}
			else
			{
				wentForwardAlready = false;

				if (direction < 5)
				{
					motor[left] = maxSpeed * -1;
					motor[right] = maxSpeed * 1;
					lastLeft = -1;
				}
				else if (direction > 5)
				{
					motor[left] = maxSpeed * 1;
					motor[right] = maxSpeed * -1;
					lastLeft = 1;
				}
				else if (direction == 5)
				{
					motor[left] = maxSpeed;
					motor[right] = maxSpeed;
					clearTimer(T1);
					while (!hasArrived() && time1[T1] < 3000)	{}
				}
			}
		}

		if (hasArrived())
		{
			motor[left] = 0;
			motor[right] = 0;
			const int height = 120 + amountAboveTubes; // So that we have room for dumping BALLS
			motor[scoop] = -87;
			const int ballSpittingOutTime = 750;
			wait1Msec(ballSpittingOutTime);
			liftHeight(-133);
		}

	}

}
